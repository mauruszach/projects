# Integrated Polymarket project

This is a deployment snapshot of `/Users/zachmaurus/Desktop/polymarket_spd`, copied on 2026-09-06. It includes the frontend, bundled graph and prices, and the Python refresh pipeline. No credentials, caches, virtual environments, or research result directories were copied.

Integration changes: the frontend uses `/polymarket` as its base path, browser requests include this prefix, and navigation includes a return to the project chooser. The original project is unchanged. Future original-project changes must be deliberately synchronized into this snapshot.
