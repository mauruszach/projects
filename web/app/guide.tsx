const chapters=[['start','Your first exploration'],['data','Where the data comes from'],['graph','Reading the knowledge graph'],['key','Your API key and costs'],['simulation','What a simulation does'],['matrix','Reading the payoff matrix'],['equilibrium','Checking an equilibrium'],['statistics','Statistics and forecasting'],['limits','Assumptions and limitations']];
export default function Guide(){return <div className="guide-layout"><aside className="guide-toc"><p className="eyebrow">HOW IT WORKS</p>{chapters.map(([id,title])=><a key={id} href={`#${id}`}>{title}</a>)}</aside><article className="guide-article"><p className="eyebrow">METHOD & TRANSPARENCY</p><h1>From reported events to inspectable scenarios.</h1><p className="lead">This workspace connects geopolitical actors and events, then uses a language model to propose strategic choices. Its purpose is to make assumptions visible so you can examine them.</p><p className="notice">The simulation is an exploration of hypothetical behavior. Its payoffs and chosen outcomes are not calibrated forecasts, and are not observed facts.</p>
<section id="start"><h2>Your first exploration</h2><ol><li><strong>Check the evidence status.</strong> The top strip tells you whether the graph contains recorded events or synthetic demo data, and when the service was last checked.</li><li><strong>Select an event.</strong> Click a diamond in the graph or choose an entry in the event list. Its actors, timestamp, Goldstein score and source appear beside the graph. A simulation needs two distinct actors.</li><li><strong>Add your key and run one step.</strong> Start with the default one-step run. Choose a cooperative, escalatory or unconstrained scenario hint.</li><li><strong>Read the matrix.</strong> Compare both actors’ options and payoffs. Inspect the model’s choice, the independent equilibrium check and the reasoning.</li><li><strong>Trace the hypothetical event.</strong> A new outlined diamond is added to the graph. Its dashed connection leads back to the event used as its starting point.</li></ol></section>
<section id="data"><h2>Where the data comes from</h2><p>The backend ingestion pipeline downloads bounded GDELT event exports, transforms CAMEO-coded rows into actors, events and locations, and writes them to Neo4j. GDELT describes events extracted from news coverage; it is not a complete account of everything that happened.</p><p>The website asks the event service for up to 500 records every 15 seconds. This watcher is always enabled while the workspace is mounted, including while you read this guide. The ingestion schedule is separate: checking the service does not itself download new GDELT exports.</p><table><thead><tr><th>Label</th><th>What it means</th></tr></thead><tbody><tr><td>Recorded</td><td>A record returned by the event service. Consult its original report when a source link is present.</td></tr><tr><td>Demo</td><td>A synthetic sample used when recorded data has not loaded. It is not current news.</td></tr><tr><td>Simulated</td><td>A hypothetical event generated during a run. It is never included in the evidence average.</td></tr></tbody></table><p>If the service is unavailable, the watcher keeps trying. The interface retains the last successful snapshot, or continues showing the demo if none has loaded. A successful check is a service refresh time, not a claim that every event is recent.</p></section>
<section id="graph"><h2>Reading the knowledge graph</h2><p>Circles represent actors: countries, organizations and people. Diamonds represent events. Events can exist even when their actor fields are incomplete. The backend also stores locations; this view shows a selected event’s location in its details rather than drawing location nodes.</p><div className="formula">Actor → INITIATED → Event → TARGETED → Actor<br/>Event → OCCURRED_AT → Location</div><p>Selecting a node highlights its connections without removing the surrounding network. The search field intentionally narrows visible records. Drag nodes to rearrange them, drag the background to pan, or use the zoom controls. Position and distance are layout choices, not geographic distance, influence, or causal strength.</p><p>Event timestamps live on event nodes. Dashed scenario connections express generation history, not an established causal relationship. Source reports belong to recorded events; generated events do not invent source links.</p></section>
<section id="key"><h2>Your API key and costs</h2><p>Reading the graph and the evidence statistics needs no Anthropic key. Generating a matrix does. Add your own key in the first setup panel; the host does not provide a shared key for visitors.</p><p>The key is held in the page’s memory, not saved to browser storage. You can clear it when no run is active, and reloading clears it. When you run, the browser sends the key to this application’s server in a request header; that server uses it to call Anthropic. This is not a browser-to-Anthropic direct connection.</p><p>Entering a key does not verify it or make a paid request. Validation happens when a run is attempted. One step requests one model generation; transient failures can cause retries. The wrapper can reuse a recent identical response in a short-lived cache isolated by a hashed key identifier. The interface does not estimate your bill or token balance.</p><p>Start with one step. Larger runs request successive steps only after the previous one succeeds. “Stop after this step” keeps the current response and prevents the next request; it cannot undo usage for a request already sent.</p></section>
<section id="simulation"><h2>What a simulation does</h2><ol><li><strong>Freeze the starting evidence.</strong> The run takes the selected event, its actor pair and loaded evidence dated at or before that starting event. Later feed updates remain visible but do not silently change this run’s evidence snapshot. The starting event remains on the graph even if it leaves the feed snapshot.</li><li><strong>Prepare context.</strong> Up to 12 recent related events, ordered by timestamp, are sent with the current base event. Prior hypothetical steps can enter this context, labeled as simulated.</li><li><strong>Ask for strategic alternatives.</strong> Claude proposes actions for both actors, a payoff for every combination, an equilibrium explanation and one chosen joint outcome. The scenario hint guides this hypothetical exercise; it is not an estimated likelihood.</li><li><strong>Validate the response.</strong> The server checks the matrix dimensions, finite numeric payoffs, action membership, explanatory text and the Goldstein estimate’s range. Validation checks structure, not whether the strategic assumptions are true.</li><li><strong>Show the complete result.</strong> The interface displays the matrix and adds a hypothetical event to the graph, preserving the actor order used in the matrix. A later step starts from this generated event.</li></ol><p>Waiting status shows elapsed time and the step number. There is no pretend progress through internal reasoning stages. A failed request stops the run and displays its error; completed results remain available in the step selector.</p><p>Hypothetical timestamps advance 15 minutes from the previous step for sequence ordering only. They are not timing predictions. Session results are held in the page and disappear on reload; they are not written into the recorded Neo4j event store.</p></section>
<section id="matrix"><h2>Reading the payoff matrix</h2><p>The first actor chooses a row. The second chooses a column. Their combination identifies one cell. The first number, shown in blue, is the row actor’s payoff; the second number, shown in black, is the column actor’s payoff.</p><div className="formula"><strong className="blue">3</strong>, <strong>−2</strong> = an assumed benefit of 3 for the row actor and an assumed cost of 2 for the column actor.</div><p>The model is asked to use an illustrative −5 to +5 scale. Higher is more favorable for that actor. These are proposed utility scores, not dollars, casualties, measured preferences or percentages. A payoff of 3 does not imply a 30% chance, and adding the two numbers does not establish the socially best outcome.</p><p>The outlined blue cell marks the model’s chosen combination. “Pure equilibrium” is a separate annotation computed from the returned numbers. A chosen outcome need not pass that check; the interface calls out the difference instead of treating the model’s explanation as proof.</p></section>
<section id="equilibrium"><h2>Checking an equilibrium</h2><p>A best response is the most favorable action available to one actor when the other actor’s action is held fixed. The interface independently checks each cell:</p><ol><li>Hold the column fixed. Is the first payoff at least as large as the first payoff in every other row of that column?</li><li>Hold the row fixed. Is the second payoff at least as large as the second payoff in every other column of that row?</li><li>If both conditions hold, mark a pure-strategy Nash equilibrium. Ties are allowed, so multiple cells can qualify.</li></ol><div className="formula">At cell (i, j):<br/>u₁(i, j) ≥ u₁(k, j) for every alternative row k<br/>u₂(i, j) ≥ u₂(i, l) for every alternative column l</div><p>No qualifying cell means no pure equilibrium in this matrix. This interface does not solve for mixed strategies or assign probabilities to outcomes. An equilibrium is conditional on the proposed actions and payoffs; it is not evidence that real actors will choose it.</p></section>
<section id="statistics"><h2>Statistics and forecasting</h2><p>The evidence panel reports the number of events, distinct actor codes and the arithmetic mean Goldstein score across the loaded evidence snapshot. Simulated events are excluded.</p><div className="formula">Mean Goldstein = sum of evidence event scores ÷ evidence event count</div><p>Goldstein scores encode a cooperation–conflict interpretation of event types, typically from −10 to +10. Averaging them summarizes this sample’s coded activity. It does not correct for uneven news coverage, reporting duplication, missing actors or differences in event importance.</p><p>The Python project has a separate statistical forecasting path based on recent event frequency, tone, Goldstein trends and graph features. That model and its historical evaluation are distinct from this language-model simulation. This workspace does not currently display its calibrated forecast probabilities. The payoff matrix must not be presented as a substitute for backtesting or a calibration report.</p></section>
<section id="limits"><h2>Assumptions and limitations</h2><ul><li><strong>Coverage is bounded.</strong> The loaded snapshot is not a comprehensive world model, and missing reports do not establish that nothing happened.</li><li><strong>Identity is imperfect.</strong> CAMEO codes and names can be ambiguous; distinct codes may refer to related or duplicate entities.</li><li><strong>Payoffs are assumptions.</strong> The model may invent plausible motivations, omit actions or be internally inconsistent. The independent check verifies only the numbers it supplied.</li><li><strong>Sequential errors compound.</strong> Later generated steps can build on earlier invented events. More steps do not make a scenario more credible.</li><li><strong>Two actors simplify reality.</strong> Domestic politics, third parties, private information and changing incentives are not fully represented by a single two-player matrix.</li><li><strong>No live research agent.</strong> This simulation uses supplied event context. It does not independently search the web, verify every source or measure real-world payoffs.</li></ul><p>Use the workspace to ask better questions about strategic assumptions. Assess actual forecast skill separately with historical holdouts, appropriate baselines and calibration measurements.</p></section>
</article></div>}
