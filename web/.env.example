# Optional: the hosted Predictor FastAPI service (no trailing slash).
# Without it, the graph uses labeled demo data until recorded events are available.
GDELT_API_URL=http://127.0.0.1:8000
# Internal Polymarket address is fixed by the combined container launcher.
# Do not add an operator Anthropic key: visitors bring their own.
